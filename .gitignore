# Binaries
bin/
dist/
amnesiai
amnesiai.exe

# Node (semantic-release tooling)
node_modules/

# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
vendor/
go.work
go.work.sum

# GoReleaser
dist/

# Build cache
/tmp/

# Secrets & credentials
secrets.hcl
secrets.local.*
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
auth.json
.credentials.json

# amnesiai local config lives at ~/.amnesiai/config.toml — it is outside the
# repo root and cannot be committed. No .gitignore entry needed here.

# Claude Code local IDE state — contains machine-local tool permissions and
# internal path references that must never be committed.
.claude/

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# IDEs
.idea/
.vscode/
*.swp
*.swo
*~

# Test coverage
coverage.out
coverage.html

# Testdata generated files
testdata/output/

# goreleaser local config override
.goreleaser.local.yaml
