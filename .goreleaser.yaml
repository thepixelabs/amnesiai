# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
version: 2
project_name: amnesiai

before:
  hooks:
    - go mod tidy
    - go generate ./...

builds:
  - env: [CGO_ENABLED=0]
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    ldflags:
      - -s -w
      - -X github.com/thepixelabs/amnesiai/internal/version.Version={{.Version}}
      - -X github.com/thepixelabs/amnesiai/internal/version.Commit={{.Commit}}
      - -X github.com/thepixelabs/amnesiai/internal/version.Date={{.Date}}

archives:
  - format: tar.gz
    format_overrides:
      - goos: windows
        format: zip
    name_template: "{{.ProjectName}}_{{.Os}}_{{.Arch}}"

checksum:
  name_template: "checksums.txt"

snapshot:
  version_template: "{{incpatch .Version}}-next"

release:
  # semantic-release already created the release with notes; just add assets.
  mode: keep-existing

changelog:
  sort: asc
  filters:
    exclude: ["^docs:", "^test:", "^chore:"]

brews:
  - name: amnesiai
    repository:
      owner: thepixelabs
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_APP_TOKEN }}"
    homepage: "https://amnesiai.pixelabs.net"
    description: "git your ai setup — backup and restore AI coding assistant configurations"
    license: "MIT"
    install: |
      bin.install "amnesiai"
    test: |
      system "#{bin}/amnesiai", "version"
